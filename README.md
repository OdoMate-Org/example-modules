# OdoMate — Example Modules

Real Odoo modules generated by [OdoMate](https://www.odomate.pro) — an AI-driven Odoo module
generator that turns business specifications into OCA-aligned Odoo 19 modules with tests,
a live demo, and a downloadable zip.

Every module in the [example modules](#example-modules) table was generated from a written
business specification by the OdoMate team, security-reviewed by a human, and published
unedited apart from publication metadata (store banner reference).
Each module folder contains a `SPEC.md` with the original business specification it was
generated from — so you can judge the input against the output yourself.

The repository also hosts [OdoMate tools](#odomate-tools) — first-party modules we wrote by
hand rather than generated. They are clearly separated below, so nothing here is mistaken
for generator output.

> OdoMate is in closed beta — [request access](https://www.odomate.pro#waitlist).

## Example modules

| Module | Description | Complexity |
|---|---|---|
| [`odomate_project_checklist`](odomate_project_checklist/) | Reusable step-by-step checklists on Project tasks: template library, per-task customization, live progress bar (cancelled steps excluded), auto-stamped start/end dates with self-healing, and a replace-confirmation wizard. 12 unit tests. | Medium — 5 models incl. an inherited `project.task`, stored computes, a TransientModel wizard, record rules |
| [`odomate_product_kind`](odomate_product_kind/) | Hierarchical Product Kind classification carrying production semantics (route type, manufacturing mode) from products through sales into manufacturing — reclassify a product anytime and its confirmed sale order lines and manufacturing orders still keep the kind they had at confirmation. 22 unit tests. | Medium-High — 1 new hierarchical model (partial unique indexes, model-level snapshot immutability) plus inherits on `product.template`, `mrp.bom`, `mrp.production`, `sale.order.line`, and a self-healing `sale.report` search-view patch via install/upgrade hooks |
| [`sale_discount_approval_21072026`](sale_discount_approval_21072026/) | Order- and invoice-level discount (Percent or fixed Amount) that fans out evenly to every line, with a company-level two-step approval gate — orders whose average line discount exceeds a configurable limit move to Waiting Approval until a Sales Manager approves or rejects them. 30 unit tests. | Medium — no new models; inherits `sale.order`, `sale.order.line`, `account.move`, `account.move.line`, `res.company`, `res.config.settings`, a custom order state + manager-gated approval workflow, and QWeb injections into 4 print/portal/analysis templates |
| [`sale_warehouse_auto_fulfillment`](sale_warehouse_auto_fulfillment/) | Three per-warehouse switches turn one Confirm click on a sales order into a full fulfillment run — delivery validated, customer invoice created, invoice posted — with the whole run in a single savepoint, so a real failure leaves the order a quotation with no stray delivery or orphan invoice, and the reason in a plain-language blocking message. 12 unit tests. | Low-Medium — no new models; 3 Boolean fields on `stock.warehouse` and one `sale.order.action_confirm()` override, with savepoint rollback and an out-of-transaction `ir.logging` write for the failure trail |
| [`audit_trail`](audit_trail/) | Configurable audit trail on any model: nominate a kind of record per company, choose which of create, change, delete, export and view get recorded, and read back who did what with the old value beside the new one — plus optional full snapshots of deleted records, working-session and screen attribution, and a retention clean-up that is off until you switch it on. 43 unit tests. | Medium-High — 6 new models and an `_inherit = "base"` override that hooks create/write/unlink/export/read on every model in the database without patching ORM internals, company-scoped record rules on all five data models, a TransientModel settings wizard, and a self-retriggering batched retention cron that also retires the sessions and screens it empties |
| [`odomate_hr_employee_info_06092026_2`](odomate_hr_employee_info_06092026_2/) | Family and dependant records on the employee file, with exactly one emergency contact enforced in the database as well as the form and mirrored onto the employee's own emergency fields; expiry dates and scans for the identification reference and the passport; a daily reminder that fires once, on the day the configured lead time is reached, instead of every morning until the document expires; a joining date computed from the employment history; and an employee record created automatically for every new internal login. 29 unit tests. | Medium — 2 new models plus inherits on `hr.employee`, `hr.version`, `res.users`, `res.company` and `res.config.settings`; a partial unique index and `create`/`write` guards behind the single-emergency-contact rule, company record rules on the dependant data, an edge-triggered daily cron with two editable mail templates, and a stored compute that ignores writes |
| [`odomate_hr_documents`](odomate_hr_documents/) | Employee documents that expire — work permits, driving licences, first-aid certificates — each held once with its own reference, its issue and expiry dates and its scans, so the renewal that needs six weeks is not discovered with five days left. A daily scheduled run expires whatever has lapsed and chases on one of four configurable patterns, emailing the holder and refreshing a dated to-do on their manager rather than stacking up duplicates; renewing moves the superseded scans onto a history row instead of overwriting them, so what covered which period stays answerable. One valid document per type is enforced by a partial unique index as well as by the form, and an ordinary employee can read their own documents and no one else's. 54 unit tests. | Medium — 4 new models and a TransientModel wizard, plus a stat button on `hr.employee`; six record rules including a personal read rule on both data models, a searchable computed days-remaining label carrying its own search method, a daily cron with an editable mail template and manager activities, and `ir.attachment` records re-pointed between records on renewal |
| [`odomate_hr_custody`](odomate_hr_custody/) | A register of the company property people take away with them — laptops, phones, vehicle keys, access cards, instruments — with a request, approval and return trail over each one. Approving an item somebody already holds is refused outright and the refusal names the holder, so the same laptop is never promised to two people; a refusal must carry a written reason, and an extension records the new return date without destroying the one originally promised, which is the only measure of lateness. A daily scheduled run emails the holder from the day before the return date onwards. Every approved request prints a one-page handover document carrying the holder, the item, an undertaking and two signature lines. An ordinary employee sees their own requests and no one else's, and cannot approve. 33 unit tests. | Medium — 2 new models and two purpose-bound TransientModel wizards, plus two stat buttons on `hr.employee`; four record rules including a personal read rule, a double-booking guard raised at approval, a daily cron with an editable mail template, a from-scratch QWeb PDF report, and non-stored computes for current holder and overdue state |
| [`odomate_hr_salary_advance`](odomate_hr_salary_advance/) | An employee asks for part of their salary before payday. This records the request, checks it against a policy the company sets once — a share of the monthly wage, an optional hard ceiling, and whether a second advance may be open at the same time — and shows the four figures behind that decision live on the form before it is even saved. Approving is not paying: the Pay step creates and posts a real outbound payment on a configured journal, and refuses outright if no journal is set or the employee has no work contact rather than guessing at either. The outstanding balance then lands on the next payslip as its own deduction line, in a batch payroll run as well as on the form, and what the confirmed line actually took is what gets recorded — reduce it to spread the repayment over two months and the advance stays open for the remainder, closing itself at zero. A refusal must carry a written reason; refused and cancelled are dead ends, and neither reports a balance the company is owed. An HR officer can disburse an advance and still cannot open the journal entry behind it. Requires the OCA `payroll` module. 55 unit tests. | Medium–High — 1 new model plus two purpose-bound TransientModel wizards, extending `hr.payslip`, `hr.payslip.input`, `hr.employee`, `res.company` and `res.config.settings`; OCA payroll integration through a shipped-but-unattached salary rule reached from both the onchange and the batch compute path, `account.payment` creation under a `sudo()` scoped to one call, an install-time hook that resolves the deduction category instead of referencing a demo-only xmlid, a personal read rule and a global multi-company rule |
| [`odomate_hr_leave_email`](odomate_hr_leave_email/) | One published address turns an employee's email into an ordinary Time Off request — same approval flow, same calendar, same reporting, with the original message sitting in the request's own thread so anyone can read what was actually sent. Nothing is guessed, and that is the whole design: a message whose sender cannot be matched to an employee, whose dates cannot be read, whose dates run backwards, or whose company has no usable Time Off type configured creates **no request at all**. It is refused instead, recorded in an *Email Requests* log under Time Off → Reporting with the three refusal reasons distinguished from each other, and answered with the accepted formats — because a half-filled request sitting in the approval queue looking real is worse than none. Dates are read from the body as `YYYY-MM-DD` or `DD/MM/YYYY`, first date to second, a single date meaning one day; the subject becomes the description. The gateway is never allowed to fail on a bad message: every stage runs inside a savepoint, and with the feature switched off Odoo's own handling runs in one too. 31 unit tests. | Low-Medium — 1 new model and a single `message_new` override on `hr.leave` that adds no state and no approval step of its own, plus inherits on `res.company` and `res.config.settings`; a shipped `mail.alias` restricted to authenticated employees, sender resolution across user login, user email and `work_email` with a multi-company tiebreak, a global multi-company record rule that still leaves unresolved senders visible to Time Off staff, an editable refusal mail template, and a settings block that detects a missing `mail.alias.domain` and says so instead of showing an unusable address |
| [`odomate_hr_transfer`](odomate_hr_transfer/) | An employee moves — a new department, job title, office, manager or company. Instead of typing over the employee record and losing what was there, this writes the move down: one record with its own reference, the date it takes effect, a reason, and an approval step an HR Manager has to pass — enforced inside the method, so a script or an API call is refused exactly like the button is. Applying creates a **new dated version** on the employee rather than editing the existing one, and freezes the before-values at that moment, so the record still answers "moved from where" after the employee changes again. A daily scheduled run applies approved transfers when their date arrives; before then the form says so instead of offering a button that would be wrong. Once applied, the employee, the date, the status and every target lock, and reversing a move means recording another one. A move between group companies keeps one employee record, so the time-off balance and the years of service survive it. 32 unit tests. | Medium — 1 new model plus a purpose-bound TransientModel wizard and a stat button on `hr.employee`; a stored snapshot written inside the same transaction as the `hr.version` it creates, a write guard that locks an applied record, an approval gate checked in Python as well as by groups, a global multi-company rule, and a daily cron that runs each transfer in its own savepoint |
| [`odomate_hr_resignation`](odomate_hr_resignation/) | Somebody resigns, and from that moment a list of things has to happen in an order, involving five different people. This writes the list down: one record from the day notice is given, a last working day worked out from that employee's own notice period and then left alone the instant a person types a negotiated date over it, an approval trail with the manager's step switchable per company, and a clearance checklist built from a template with each line routed to whoever owns it. The point of the module is the release: it re-reads the company-property register **at the moment somebody presses the button** and refuses while anything is still out, naming the item, its handover reference and the date it was due back — so the checklist cannot be ticked past a laptop nobody returned. Releasing writes the departure, ends the current contract, archives the employee and their login in one transaction, and the record then locks in code rather than merely on screen. An ordinary user has no menu for any of it, and the leaver's own manager reaches exactly the one record they must approve. 38 unit tests. | Medium — 3 new models and a purpose-bound TransientModel wizard, plus a stat button on `hr.employee`; a partial unique index behind one-active-resignation-per-employee, a live `search_count` against another generated module's register at release time beside a deliberately stale snapshot taken at approval, a personal record rule on the clearance lines and a two-hop manager rule on the resignation itself, a write guard that locks a released record, a single `sudo()` scoped to archiving the login, and a daily cron running the same two checks as the button |
| [`odomate_hr_notices`](odomate_hr_notices/) | Telling everyone something, and being able to show who was told. HR writes an announcement, picks who it is for — everybody, named people, whole departments, or a job position — and it goes out only after an HR Manager publishes it; refusing it needs a written reason, and no transition skips a step. Published notices reach people **inside Odoo**, as a counter in the top bar rather than an email nobody opens: it lists what is waiting, most urgent first, and refreshes on page load and on opening, with no polling and no background traffic. Pressing Acknowledge is idempotent and records one thing — that the button was pressed — and the module says so on its own screens rather than letting it be read as proof of reading. HR sees acknowledged, audience size, and the difference **as a list of names**. Separately, a saved date reminder answers one kind of question — "which work permits expire in the next 30 days" — with a live count that runs as you, so it never surfaces records you may not read. A daily run expires notices past their end date; they stay readable in HR's records and leave everyone else's. 45 unit tests. | Medium-High — 3 new models plus a refusal wizard and an `hr.employee` extension; one audience-membership method reused by the record rule, the systray, the employee stat button and the pending list, an approval gate checked inside the method as well as by groups, field-level `groups=` on every acknowledgement field, a whitelisted-model domain builder that runs as the calling user, an OWL systray component (64 lines of JavaScript) registered in the systray registry, multi-company record rules on every model, and a daily expiry cron |

## OdoMate tools

First-party modules, **written by hand rather than generated**. They are published here for
the same reason the examples are — so you can read them before you trust them — and because
this is the repository the Odoo Apps Store scans.

| Module | Description |
|---|---|
| [`odomate_connector`](odomate_connector/) | Exports a redacted structural snapshot of your database (`odomate_context.json`) so OdoMate can generate modules that fit your real environment and test them against a replica of it. Structural context only — installed modules, models, fields, customized views, settings, group user *counts* and record *counts*; never business records, user data or credentials, and no network calls. 69 unit tests. |

## Repository structure

This repository follows the standard Odoo community convention (the same one OCA and the
Odoo Apps Store use):

- **Branches are named after Odoo series** — you are on `19.0`, the current target version.
  When OdoMate supports Odoo 20, a `20.0` branch will appear.
- **Each top-level directory is one installable module** in standard Odoo layout
  (`__manifest__.py`, `models/`, `views/`, `security/`, `tests/`, …).
- **`SPEC.md` inside each module** documents the business specification the module was
  generated from.

## Installing a module

Clone the branch matching your Odoo version and copy the module folder into your addons
path, or download a folder directly:

```bash
git clone --branch 19.0 https://github.com/OdoMate-Org/example-modules.git
cp -r example-modules/<module_name> /path/to/your/addons/
```

Then update the apps list in Odoo and install as usual. Modules target **Odoo 19
Community edition**.

## What these examples are (and aren't)

- They are **real output** — generated from the spec shown in each `SPEC.md`, with the
  generated tests included.
- They pass their automated test suites and install cleanly on Odoo 19 Community.
- They are **not** a substitute for your own review: OdoMate's own guidance is that a human
  reviews generated code before production deployment. These modules went through exactly
  that review.
- None of them contain customer data or beta-user work — all showcase modules are
  team-authored specifications.

## License

All modules are licensed under [LGPL-3](LICENSE) — the license OdoMate emits by default
(the same one Odoo uses for Community). Each module's `__manifest__.py` states its license
explicitly.

## Links

- Website: https://www.odomate.pro
- Blog: https://www.odomate.pro/blog
- LinkedIn: https://www.linkedin.com/company/odomate-ai
- YouTube: https://www.youtube.com/@odomate-ai
- X: https://x.com/OdoMateAI
